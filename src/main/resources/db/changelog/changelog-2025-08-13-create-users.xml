<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.31.xsd">

    <changeSet id="create-users-table" author="Shulikov Vladislav">

        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="users"/>
            </not>
        </preConditions>

        <createTable tableName="users">
            <column name="uuid" type="UUID">
                <constraints primaryKey="true" nullable="false" unique="true"/>
            </column>

            <column name="full_name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="phone_number" type="VARCHAR(20)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="avatar" type="VARCHAR(512)"/>

            <column name="role_uuid" type="UUID"/>

            <column name="created_at" type="TIMESTAMP"/>
            <column name="updated_at" type="TIMESTAMP"/>
        </createTable>

        <createIndex indexName="idx_users_uuid" tableName="users">
            <column name="uuid"/>
        </createIndex>

        <createIndex indexName="idx_users_phone_number" tableName="users">
            <column name="phone_number"/>
        </createIndex>

        <createIndex indexName="idx_users_created_at" tableName="users">
            <column name="created_at"/>
        </createIndex>

        <addForeignKeyConstraint
                constraintName="fk_users_role_uuid"
                baseTableName="users"
                baseColumnNames="role_uuid"
                referencedTableName="roles"
                referencedColumnNames="uuid"
                onDelete="CASCADE"/>

        <rollback>
            <dropForeignKeyConstraint baseTableName="users" constraintName="fk_users_role_uuid"/>
            <dropIndex indexName="idx_users_uuid" tableName="users"/>
            <dropIndex indexName="idx_users_phone_number" tableName="users"/>
            <dropIndex indexName="idx_users_created_at" tableName="users"/>
            <dropTable tableName="users"/>
        </rollback>

    </changeSet>
</databaseChangeLog>