<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.31.xsd">

    <changeSet id="create-roles-table" author="Shulikov Vladislav">

        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="roles"/>
            </not>
        </preConditions>

        <createTable tableName="roles">
            <column name="uuid" type="UUID">
                <constraints primaryKey="true" nullable="false" unique="true"/>
            </column>
            <column name="role_name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex indexName="idx_roles_uuid" tableName="roles">
            <column name="uuid"/>
        </createIndex>

        <rollback>
            <dropIndex indexName="idx_roles_uuid" tableName="roles"/>
            <dropIndex indexName="idx_roles_user_id" tableName="roles"/>
            <dropTable tableName="roles"/>
        </rollback>

    </changeSet>
</databaseChangeLog>